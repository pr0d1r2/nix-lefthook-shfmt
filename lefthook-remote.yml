---
pre-commit:
  commands:
    shfmt:
      glob: "*.sh"
      run: timeout ${LEFTHOOK_SHFMT_TIMEOUT:-30} shfmt -d -i 4 -ci {staged_files}

pre-push:
  commands:
    shfmt:
      glob: "*.sh"
      run: timeout ${LEFTHOOK_SHFMT_TIMEOUT:-30} shfmt -d -i 4 -ci {push_files}
